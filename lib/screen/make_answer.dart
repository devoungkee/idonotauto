import 'package:flutter/material.dart';

class Make_Answer extends StatelessWidget {
  const Make_Answer({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child:Text('Here is answers'),
    );
  }
}
